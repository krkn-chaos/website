# Generated by krkn-doc-sync-bot. Do not edit by hand.
source_repo: krkn-hub
source_ref: HEAD
params:
- name: TOTAL_CHAOS_DURATION
  description: set chaos duration (in sec) as desired
  description_source: published-table
  type: number
  default: '60'
  required: false
- name: NODE_SELECTOR
  description: defines the node selector for choosing target nodes. If not specified, one schedulable node in the cluster will be chosen at random. If multiple nodes match the selector, all of them will be subjected to stress.
  description_source: published-table
  type: string
  default: ''
  required: false
- name: NAMESPACE
  description: Namespace where the scenario container will be deployed
  description_source: krknctl
  type: string
  default: default
  required: false
- name: INSTANCE_COUNT
  description: restricts the number of selected nodes by the selector
  description_source: published-table
  type: number
  default: '1'
  required: false
- name: EXECUTION
  description: sets the execution mode of the scenario on multiple nodes, can be parallel or serial
  description_source: published-table
  type: enum
  default: parallel
  required: false
- name: INGRESS
  description: sets the network filter on incoming traffic, can be true or false
  description_source: published-table
  type: boolean
  default: 'false'
  required: false
- name: EGRESS
  description: sets the network filter on outgoing traffic, can be true or false
  description_source: published-table
  type: boolean
  default: 'false'
  required: false
- name: INTERFACES
  description: a list of comma separated names of network interfaces (eg. eth0 or eth0,eth1,eth2) to filter for outgoing traffic
  description_source: published-table
  type: string
  default: ''
  required: false
- name: PORTS
  description: a list of comma separated port numbers (eg 8080 or 8080,8081,8082) to filter for both outgoing and incoming traffic
  description_source: published-table
  type: string
  default: ''
  required: false
- name: SCENARIO_TYPE
  description: Plugin key krkn dispatches the scenario on. Fixed by the container image, not a knob to change.
  description_source: built-in
  default: network_chaos_ng_scenarios
  required: false
- name: SCENARIO_FILE
  description: Path to the scenario file baked into the container image. Fixed by the image, not a knob to change.
  description_source: built-in
  default: scenarios/kube/network-filter.yml
  required: false
- name: IMAGE
  description: The network chaos injection workload container image
  description_source: krknctl
  type: string
  default: quay.io/krkn-chaos/krkn-network-chaos:latest
  required: false
- name: PROTOCOLS
  description: a list of comma separated protocols to filter (tcp, udp or both)
  description_source: published-table
  type: string
  default: tcp
  required: false
- name: NODE_NAME
  description: the node name to target (if label selector not selected)
  description_source: published-table
  type: string
  default: ''
  required: false
- name: TAINTS
  description: 'List of taints for which tolerations need to be created. Example: ["node-role.kubernetes.io/master:NoSchedule"]'
  description_source: published-table
  type: string
  default: ''
  required: false
- name: SERVICE_ACCOUNT
  description: optional service account for the Node Network Filter workload
  description_source: published-table
  type: string
  default: ''
  required: false
