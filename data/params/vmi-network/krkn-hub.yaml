# Generated by krkn-doc-sync-bot. Do not edit by hand.
source_repo: krkn-hub
source_ref: HEAD
params:
- name: TOTAL_CHAOS_DURATION
  description: Chaos duration in seconds
  description_source: published-table
  type: number
  default: '60'
  required: false
- name: VMI_NAME
  description: Regex to match VMI names (e.g. `virt-server-.*` or `.*` for all)
  description_source: published-table
  type: string
  default: ''
  required: false
- name: LABEL_SELECTOR
  description: Label selector to filter VMIs (e.g. `app=myapp`)
  description_source: published-table
  type: string
  default: ''
  required: false
- name: NAMESPACE
  description: Namespace containing the target VMIs (required)
  description_source: published-table
  type: string
  default: default
  required: false
- name: INSTANCE_COUNT
  description: Maximum number of VMIs to target
  description_source: published-table
  type: number
  default: '1'
  required: false
- name: EXECUTION
  description: 'Execution mode: `serial` or `parallel`'
  description_source: published-table
  type: enum
  default: parallel
  required: false
- name: INGRESS
  description: Shape incoming traffic to the VM
  description_source: published-table
  type: boolean
  default: 'true'
  required: false
- name: EGRESS
  description: Shape outgoing traffic from the VM
  description_source: published-table
  type: boolean
  default: 'true'
  required: false
- name: INTERFACES
  description: Comma-separated tap interface names (empty to auto-detect)
  description_source: published-table
  type: string
  default: ''
  required: false
- name: LATENCY
  description: Artificial latency added to packets (e.g. `100ms`, `500ms`)
  description_source: published-table
  type: string
  default: ''
  required: false
- name: LOSS
  description: Packet loss percentage (e.g. `10` for 10%)
  description_source: published-table
  type: string
  default: ''
  required: false
- name: BANDWIDTH
  description: Maximum throughput cap (e.g. `100mbit`, `1gbit`)
  description_source: published-table
  type: string
  default: ''
  required: false
- name: SCENARIO_TYPE
  description: Plugin key krkn dispatches the scenario on. Fixed by the container image, not a knob to change.
  description_source: built-in
  default: network_chaos_ng_scenarios
  required: false
- name: SCENARIO_FILE
  description: Path to the scenario file baked into the container image. Fixed by the image, not a knob to change.
  description_source: built-in
  default: scenarios/kube/vmi-network-chaos.yml
  required: false
- name: IMAGE
  description: Network chaos injection workload image
  description_source: published-table
  type: string
  default: quay.io/krkn-chaos/krkn-hub:workload-krkn-network-chaos
  required: false
- name: TAINTS
  description: List of taints for which tolerations are created (e.g. `["node-role.kubernetes.io/master:NoSchedule"]`)
  description_source: published-table
  type: string
  default: ''
  required: false
- name: SERVICE_ACCOUNT
  description: Optional service account for the scenario workload
  description_source: published-table
  type: string
  default: ''
  required: false
